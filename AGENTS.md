# Codex 项目背景

## 项目定位

当前项目是一个中国 A 股量化交易回测系统，用于验证交易策略在历史行情数据上的收益、风险、回撤和交易表现。

项目核心不是电商推荐服务，也不是 Java 后端服务，而是一个轻量级 Python 回测框架。

## 协作偏好

- 使用中文回答所有问题。
- 解释代码时优先结合量化回测业务语义说明。
- 修改代码前先理解现有模块边界，避免无关重构。
- 新增核心逻辑时尽量补充测试，覆盖正常场景和边界场景。
- Python 代码保持清晰类型提示，关键业务逻辑添加中文注释。

## 核心模块

- `backtest/data.py`：行情数据管理，负责加载、存储和按日期查询股票数据。
- `backtest/strategy.py`：策略抽象基类，定义策略生成交易信号的接口。
- `backtest/strategies.py`：内置策略集合，包括买入持有、双均线、动量、均值回归等策略。
- `backtest/engine.py`：回测引擎，负责模拟交易、现金持仓更新、手续费和滑点计算。
- `backtest/analysis.py`：绩效分析，负责计算收益率、夏普比率、最大回撤、波动率、胜率等指标。
- `backtest/fetcher.py`：A 股行情和基础数据获取封装。
- `backtest/industry.py`：行业分类和股票行业归属管理。
- `examples/`：示例脚本。
- `tests/`：单元测试。

## 业务规则重点

- A 股买卖数量按 100 股一手处理。
- 回测需考虑佣金、印花税、过户费和滑点。
- 策略信号含义：
  - 正数表示买入。
  - 负数表示卖出。
  - 0 表示不操作。
- 回测结果重点关注总收益率、年化收益率、最大回撤、夏普比率、波动率和交易次数。

## 常用命令

```bash
pytest
python examples/simple_example.py
```

## 验收规范

- Python 代码修改完成后，优先执行 `pytest`。
- 示例或回测流程变更后，执行 `python examples/simple_example.py` 做基础链路验证。
- 不提交缓存、临时文件或无关格式化变更。

